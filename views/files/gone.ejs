<% const content = `
  <main>
    <h1>${t('files.gone.title')}</h1>
    <p>${t('files.gone.text')}</p>
    <p><a href="/files/new">${t('files.gone.new')}</a></p>
  </main>
` %>
<%- include('../layout', { content, title, crumb: t('crumb.files') }) %>
