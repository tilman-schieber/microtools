<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <%- include('../_head', { title: bringlist.data.title }) %>
</head>
<body>
  <%- include('../_header', { crumb: t('crumb.potluck') }) %>
  <main>
    <h1><%= bringlist.data.title %></h1>
    <p class="hint"><%- t('bring.show.hint') %></p>

    <div id="bringlist-content">
      <%- include('_list', { bringlist }) %>
    </div>

  </main>
</body>
</html>
