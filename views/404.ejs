<% const content = `
  <main>
    <h1>${t('notFound.title')}</h1>
    <p>${t('notFound.text')}</p>
    <p><a href="/">${t('notFound.back')}</a></p>
  </main>
` %>
<%- include('layout', { content, title: t('notFound.title'), crumb: t('crumb.notFound') }) %>
